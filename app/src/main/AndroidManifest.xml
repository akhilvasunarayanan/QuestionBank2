<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.akhi.questionbank">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".add_record" />
        <activity android:name=".modify_record" />
        <activity android:name=".exam_edit" />
        <activity android:name=".ViewExam" />
        <activity android:name=".DisplayExam" />
        <activity android:name=".AnswerKeyEdit" />
        <activity android:name=".ViewAnswerKeyEdit" />
        <activity android:name=".Evaluate"></activity>
    </application>

</manifest>